version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: pg-bookpass-new
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: holberton
      POSTGRES_DB: bookpass_db
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres-data: